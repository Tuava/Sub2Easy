# 安全、隐私与问题报告

## 数据流

- Sub2Easy 是单用户、本机工具，只绑定 `127.0.0.1`，不提供公网部署模式或多租户隔离。
- 本地 API 使用启动时生成的令牌，校验 Host/Origin，禁用跨站嵌入，响应不缓存。不应通过反向代理把它直接暴露到公网。
- 邮箱、密码、2FA 种子、Admin Key、NVT Cookie、授权结果在数据库记录内 AES-GCM 加密。主密码用 scrypt 派生密钥，不保存主密码。
- 任务ID、时间、状态、阶段、结果码等是明文元数据；加密数据库也不能上传到公开 Issue。
- 解锁后完整邮箱可见，导出文件含明文凭据；操作者必须管理好导出文件和屏幕截图。
- 密钥和解密数据存在进程内存；Python 不保证清零。本设计不防御已取得同一 OS 用户权限的恶意进程，也不代替磁盘加密。
- 忘记主密码无法恢复。尚未提供主密码轮换功能。

## 第三方访问与副作用

有效 Token JSON 导入不强制要求 NVT。需要重授权时，本程序将邮箱、密码和完整 TOTP 种子发送给固定连接器 `https://nvtokens.com`，并携带你提供的 Cookie。使用前应自行了解该服务的数据处理与账户要求；不要在公开仓库配置它。

sub2api 管理员 Key 只应发送到你配置的站点。模型测试可能产生费用，并可能使 sub2api 更新账号状态。分组变更、暂停与启用属于真实写操作；只有明确提交或开启相应自动化后执行。团队失效清理默认关闭。

## 备份与升级

1. 暂停领取新任务，等待运行任务结束，停止后端。
2. 备份整个数据目录（不仅是主 SQLite 文件；未停止时 WAL 可能仍含记录）。
3. 主密码与备份分开保存。不要在 Issue/PR 附加备份。
4. 升级前在备份副本验证；已有数据目录不会自动搬迁。
5. 更换机器或意外泄漏时，分别撤销/轮换远端 Cookie、Admin Key 和账号 Token，删除泄漏文件不能使凭据失效。

## 报告问题

公开 Issue 只提供版本、系统、固定错误码、阶段和脱敏复现步骤。不要提交真实邮箱/密码/2FA、完整响应、数据库、启动 URL、代理认证或可用 Token。

如问题涉及凭据泄漏或越权，请使用仓库 **Security → Report a vulnerability**（由仓库所有者启用 private vulnerability reporting 后可用）。如该入口尚未开启，请只公开请求一个私密报告渠道，不公开漏洞细节或敏感样本。本项目没有预设维护者私人邮箱。

开源前及发布包检查使用允许列表和模式扫描，是降低误提交风险的辅助工具，不保证发现所有形式的密钥。仍需人工复核 staged diff 和发布产物。
