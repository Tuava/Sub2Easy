# Sub2Easy — 部署与维护 AI 必读

在执行安装、部署、配置、升级、故障排查前，先阅读 [docs/ai-deployment.md](docs/ai-deployment.md) 和 [SECURITY.md](SECURITY.md)。不要只完成启动就宣称自动维护可用。

## 必须主动提醒 NVT / NV 站 CK

- 首次部署时，主动询问使用模式：仅 Token JSON 导入，还是需要账号密码/2FA 重授权和自动401修复。不要让用户自己猜还缺什么。
- 需要 NVT 授权时，必须提醒用户在本地 GUI 的“连接与模板 → NVT Cookie”填入 **NV 站的 `scm_session` Cookie 值（CK）**。没有 CK 或 CK 已失效，不能宣称自动重登已配置完成。
- 可以直接说：“请在本地工作台填写 NV 站的 scm_session Cookie（CK），不要发在聊天里；保存后我再检查是否已配置。仅有 sub2api 管理员 Key 不够完成 NVT 重授权。”
- 不把 CK、密码、2FA、Token 写进源码、命令行参数、日志、示例、截图、Issue 或 Git；不要索取粘贴在聊天里的真实材料。用户在本地密码输入框完成配置。
- `settings.has_cookie=true` 只表示已保存，不保证服务端会话有效；`connector_paused=true` 要提示更新 CK。没有显式授权，不为了验收而发起真实登录或消耗账号。
- Token-only 模式可以不使用 CK，但必须明确告诉用户：缺少 CK 或密码/2FA 时，完整自动重授权不可用。不得将这一模式报告成完整的401自恢复。

## 部署/更新约束

- 本工具只监听127.0.0.1；远程部署使用 SSH 隧道，不改成公开监听来省事。
- 服务启动时明确记录 `--data-dir`；升级必须沿用同一目录。主密码丢失无法恢复，不能新建空库冒充升级。
- 更新先检查版本，暂停新任务、等待在途任务结束、停止后台，再使用 `sub2easy-update --apply --yes --data-dir ...`。更新器只快进官方 main，不强制覆盖用户修改。
- 不执行 `git reset --hard`、删除data、批量重新登录、跳过未知写入检查来“修复更新”。更新失败报告固定错误码和实际停在哪一步。
- 自动401监控和测试组清理必须按用户选择配置；清理默认关闭，不能为了让状态变绿主动启用。
- NVT返回成功不等于账号上线；SSE完整成功和云端状态读回均通过才能宣称恢复。

## 开发检查

使用临时数据库、虚构材料和模拟上游测试。运行 Python/DOM 测试、`scripts/release_check.py`、构建和包内检查。不得读取或上传运行账号数据来充当测试夹具。
